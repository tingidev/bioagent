# Add this to the existing Caddyfile on the Hetzner server
# (alongside the datavoorelkaar.nl entries)
# Uses docker bridge IP because host.docker.internal resolves to IPv6
# which doesn't work when BioAgent binds IPv4 only.

bioagent.eu, www.bioagent.eu {
    reverse_proxy 172.17.0.1:3001
}
